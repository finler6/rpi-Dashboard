version: '3.8'

services:
  status-bot:
    build: .
    container_name: status-bot
    env_file:
      - .env
    network_mode: host
    privileged: true
    volumes:
      - /sys:/sys:ro
      - /proc:/proc:ro
      - /dev:/dev
      - ../ssh-bot-key:/root/.ssh:ro
    restart: unless-stopped

